import styles from './NestingSteps.module.css'

const NestingSteps = () => {
    return(
        <div className={styles['nesting-steps-container']}>
            <p className={styles['nesting-steps-steps-description']}>In order to Nest your DucksVegas NFT, you need to initialize a nesting account. There are some steps to Earn $QUACK:</p>
        </div>
    )
}

export default NestingSteps